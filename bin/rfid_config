#!/usr/bin/python

import pkg_resources
from shutil import copyfile
import sqlite3

def install_raspberry_pi():
  print('What path should the local database be at?')
  sqlite_path = input('-->')
  # Try actually creating the database, don't take their word for it.
  pass

def install_server():
  pass

print('Is this a Raspberry Pi installation? (Y/n)')
if input('-->').upper()[0] == 'Y':
  print('Okay, this is a Raspberry Pi!')
  install_raspberry_pi()
else:
  print('Okay, this is server')
  install_server()


